[package]
name = "generate-random-number"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.5"
rand = "0.8"
serde = { version = "1", features = ["derive"] }
tokio = { version = "1", features = ["full"] }
